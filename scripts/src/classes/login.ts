class Login{
    ValidateForm(loginForm: HTMLFontElement){
        let username = (loginForm.find("username") as HTMLInputElement).value;
    }

    ValidateUsername(username: string){
        
    }
}